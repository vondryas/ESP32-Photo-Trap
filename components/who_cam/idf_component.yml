## IDF Component Manager Manifest File
dependencies:
  idf: ">=5.4"
  espressif/esp32_p4_function_ev_board_noglib:
    version: "*"
    rules:
     - if: "target == esp32p4"
  espressif/esp_video:
    version: "^0.9.0~1"
    rules:
     - if: "target == esp32p4"
  espressif/esp_h264:
    version: "1.0.4"
    rules:
     - if: "target == esp32p4"
  espressif/esp-dl:
    version: "*"